class A is
    var a : Integer
end

class B extends A is
    var b : Integer
    this(x : Integer, y : Integer) is
	a := x
	b := y
    end
end

class Main is
    this() is
        var x : B(1, 2)
        x.set(a) := 1 // is accessible
    end
end
